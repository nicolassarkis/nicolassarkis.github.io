---
const { platform, platformName, username, icon } = Astro.props;

import "bootstrap-icons/font/bootstrap-icons.css";
---

<a href={`https://${platform}/${username}`}>{icon && <span><i class={`bi bi-${icon}`}></i> · </span>}{platformName}</a>
